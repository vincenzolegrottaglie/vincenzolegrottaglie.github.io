// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // User/org GitHub Pages site → served at the domain root, no base prefix.
  site: 'https://vincenzolegrottaglie.github.io',
});
